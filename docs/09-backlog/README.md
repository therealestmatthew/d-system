# Session backlog

[backlog.yaml](backlog.yaml) is the authoritative phase catalog. Every item contains a one-session scope, acceptance checks, verification, source-plan links, dependencies and a next action.

| Track | Scope | Source |
|---|---|---|
| `phase-rel-*` | Source integrity, failure-safe rebuilds, membership, retrieval, CI and documentation corrections | [Reliability plan](../01-plans/PLAN-004-reliability-follow-up.md) |
| `phase-html-*` | Contracts, generation, API, frontend, samples and end-to-end verification | [HTML overview](../01-plans/PLAN-003-dynamic-html-generation/PLAN-003-overview.md) and all six child plans |
| `phase-sig-*` | Six SQL signals, view lifecycle and durable velocity snapshots | [Mini-systems plan](../01-plans/PLAN-002-mini-systems-proposal.md) |
| `phase-syn-*` | Context packs, briefing, weekly review, digest and HTML export | [Mini-systems plan](../01-plans/PLAN-002-mini-systems-proposal.md) |
| `phase-mem-*` | Memory roles/contracts, candidate processing, hints, retrieval, review, pruning and conditional semantic extensions | [Memory-agent plan](../01-plans/PLAN-001-agent-memory-system.md) |
| `phase-doc-*` | Code register, allocation and catalog commands, backfill of every existing code and filename, enforcement and records | [Document code plan](../01-plans/PLAN-005-document-code-system.md) and [requirements](../06-requirements/REQ-001-document-code-requirements.md) |
| `phase-cap-*` | Entity and capture contracts, raw intake, structuring and routing, review and promotion, projection and seeding | [Capture build](../01-plans/PLAN-009-capture-build.md); the completed definition phase came from [PLAN-007](../01-plans/PLAN-007-capture-and-structuring-system.md) and [PROMPT-002](../02-prompts/PROMPT-002-capture-and-structuring-system.md) |
| `phase-ses-*` | Session type taxonomy, opening entry points, the closing protocol and dogfooding | [Session lifecycle](../01-plans/PLAN-008-session-lifecycle-protocols.md) |
| `phase-tax-*` | Session-type taxonomy investigation - the theory-first two-session investigation (theoretical model with falsifiable predictions, then the empirical test over raw transcripts and session records) feeding the CLAUDE.md/AGENTS.md revamp and a portable session-type model | [Session-type taxonomy investigation](../01-plans/PLAN-042-session-taxonomy-investigation.md) and [requirements](../06-requirements/REQ-026-session-taxonomy-investigation.md) |
| `phase-priv-*` | Structure/content boundary, identifier scrub, portfolio relocation, leak check and history rewrite before the first push | [Confidentiality sweep](../01-plans/PLAN-006-confidentiality-sweep.md) |
| `phase-gov-*` | Code-reservation enforcement, plus the governance-model and system-complexity reviews | [Code reservation enforcement](../01-plans/PLAN-010-code-reservation-enforcement.md); [Governance and complexity review](../01-plans/PLAN-014-governance-and-complexity-review.md) |
| `phase-term-*` | Shared vocabulary, generated glossaries and the architecture overview | [Terminology system](../01-plans/PLAN-012-terminology-system.md) |
| `phase-tool-*` | Per-tool operations documents and the generated tool reference | [Tooling documentation](../01-plans/PLAN-013-tooling-documentation.md) |
| `phase-idea-*` | Idea capture as an append-only event log, its write tooling and triage agent | [Idea record system](../01-plans/PLAN-016-idea-record-system.md) |
| `phase-scope-*` | Repository boundary — extracting work that belongs elsewhere and recording where it went | [Ephemeral working plans](../01-plans/PLAN-015-ephemeral-working-plans.md) |
| `phase-plc-*` | Retiring root `plans/` as a governed concept and consolidating it into `docs/01-plans/` | [Plans directory consolidation](../01-plans/PLAN-018-plans-directory-consolidation.md) |
| `phase-demo-*` | Live-demo stage backend and frontend, deterministic overview tools, overview generation and demo readiness | [Live demo plan](../01-plans/PLAN-021-live-demo.md) |
| `phase-wb-*` | Workbench — the stage becomes the chartered management UI: backend API, layout engine and notes strip, terminal rework and shells, HTML Viewer, explorers, rehearsal refresh | [Workbench plan](../01-plans/PLAN-022-workbench.md) |
| `phase-part-*` | Repeating an idea-partition sweep: the reusable prompt pack, a selectable corpus status, and the `/partition-ideas` workflow | [Repeatable idea partition](../01-plans/PLAN-025-repeatable-idea-partition.md) and [requirements](../06-requirements/REQ-009-repeatable-idea-partition.md) |
| `phase-arch-*` | Workbench architecture and quality (`P10`) — the vocabulary and its identifier migration, the duplication and structure audits, content-fit contracts, the slot configuration-schema model that supersedes `ADR-016`, sub-app packaging, the ports/process app, caching with mtime-keyed invalidation, and the three-shell terminal persistence audit. Distinct from `phase-wb-*`, which built the workbench this track revises | [Workbench architecture and quality](../01-plans/PLAN-028-workbench-architecture-quality.md) and [requirements](../06-requirements/REQ-011-workbench-architecture-quality.md) |
| `phase-wbf-*` | Workbench features and defects (`P11`) — the HTML Viewer's remaining interactions, file bookmark categories and the batch panel-bridge contract they need, the notes-strip rotator's scrolling-text and image variants, an external terminal inject/read API, and four defects on the terminal route and its frontend. Distinct from `phase-arch-*`, which revises the same product's architecture rather than its features | [Workbench features and defects](../01-plans/PLAN-027-workbench-features-defects.md) and [requirements](../06-requirements/REQ-012-workbench-features-defects.md) |
| `phase-expl-*` | Standalone explorations and housekeeping (`P12`) — an **index, not a track**. Seven unrelated phases with no shared design, no common system and no sequencing between them beyond one pair: the observability scoping pass, the content-strategy question for the owner, a `research/` capture scout, the repo tracker and its awareness agent, a non-web platform evaluation, and a websockets deep dive. `G63`'s three rehearsal placeholders were already discarded and get no phase. Six of the seven depend on nothing — the most parallelisable work in the backlog | [Standalone explorations and housekeeping](../01-plans/PLAN-037-standalone-explorations-housekeeping.md) |
| `phase-des-*` | HTML generation and design system (`P9`) — the audit that dispositions every pre-build HTML requirement and all ten `queued` `phase-html-*` phases, the governance atlas page, the template/component/palette asset libraries, and the HTML Designer agent that populates them from shipped pages. The asset work is gated on the **audit**, not on `PLAN-003`, because `PLAN-003` is what the audit examines. Supplies page assets only — whether a given report page exists is `P1`'s `phase-idg-08` ruling, a boundary reviewers check by reading a phase's `deliverables` rather than one the validator enforces | [HTML generation and design system](../01-plans/PLAN-036-html-generation-design-system.md) and [requirements](../06-requirements/REQ-021-html-generation-design-system.md) |
| `phase-sch-*` | Schema consistency and testing (`P8`) — what remains of the schema/DDL drift (the DDL and loader halves closed on their own; only a `CLAUDE.md` gap survives, and it is owner-executed), a check that catches the divergence recurring, the deferred ruling on a contract compiler, the missing `ts/` lint and test gate, a per-layer testing standard, the overview page's committed-output drift test, and deferred HTML-generation fixtures. `G36` gets **no phase** — its layout-schema test shipped and its idea is already discarded | [Schema consistency and testing](../01-plans/PLAN-035-schema-consistency-testing.md) and [requirements](../06-requirements/REQ-020-schema-consistency-testing.md) |
| `phase-proj-*` | Blocked downstream projections (`P7`) — the portfolio data question put to the owner against the real data root, deterministic as-of snapshots and two-point diffs, and two phases created `deferred` with named gates: scenario simulation and recommendation calibration. `G32` was ruled **kept** by the owner on 2026-09-13 against a decline nomination, so each carries a gate rather than a decline | [Blocked downstream projections](../01-plans/PLAN-034-blocked-downstream-projections.md) and [requirements](../06-requirements/REQ-019-blocked-downstream-projections.md) |
| `phase-ret-*` | Retrieval and knowledge infrastructure (`P6`) — retrieval-failure collection and the re-specified gate that releases four deferred `phase-mem-*` phases, the prioritised search order and its conflict rule, deterministic search, the `ADR-001` revisit and the three documentation surveys feeding **one** decision, a code-graph evaluation, and memory staleness, confidence and provenance. `G26` gets no phase here: `000004` enumerates the existing `phase-mem-*` line by id | [Retrieval and knowledge infrastructure](../01-plans/PLAN-033-retrieval-knowledge-infrastructure.md) and [requirements](../06-requirements/REQ-018-retrieval-knowledge-infrastructure.md) |
| `phase-auto-*` | Autonomous agent operations (`P5`) — the design pass that may recommend building less, the capability and approval broker enforced at the tool boundary, the external trigger gateway, the durable run ledger with resumable steps, the supervised worker and abandoned-run review, and the `000020` revisit the owner's ruling requires. The broker is built **first**, inverting the partition's ordering: a gate built after the things it gates has never gated anything. The most speculative programme in the partition | [Autonomous agent operations](../01-plans/PLAN-032-autonomous-agent-operations.md) and [requirements](../06-requirements/REQ-017-autonomous-agent-operations.md) |
| `phase-agx-*` | Agent engineering and delegation (`P4`) — truncation handling, the audit of every shipped command, skill and agent (which also rules on whether Claude Code stays a dependency), the anti-pattern store and the rules derived from it, the delegation-scoping methodology and its runtime-evidence convention, the shared state model, the agent-engineering framework derived from recorded incidents, the lifecycle roster, the expander/minimalist/arbiter trio, and independent transcript review. The roster carries **no planner role** — `phase-idg-12` in `P1` owns it. Distinct from `phase-idea-*` and `phase-idg-*`, which build agents for one domain rather than the discipline | [Agent engineering and delegation](../01-plans/PLAN-031-agent-engineering-delegation.md) and [requirements](../06-requirements/REQ-016-agent-engineering-delegation.md) |
| `phase-dgov-*` | Document and backlog governance (`P2`) — the rule for when a requirement is mandatory, document staleness defined against subject rather than age, the index-width audit, the backlog archive split, the re-prioritisation procedure, the post-hoc phase-containment check, and `_tmpagent`'s missing maturity entry. Two phases depend outside the programme: `phase-dgov-02` on `P1`'s `phase-idg-10`, and `phase-dgov-06` on `phase-gov-01`, which it collides with on file. Distinct from `phase-gov-*`, which enforces code reservation rather than the document contract | [Document and backlog governance](../01-plans/PLAN-030-document-backlog-governance.md) and [requirements](../06-requirements/REQ-015-document-backlog-governance.md) |
| `phase-conc-*` | Concurrency, git safety and enforcement (`P3`) — the stale-claim signal and its recovery procedure, the guard that refuses integration over a peer's uncommitted work, collision-proof document-code allocation, branch protection on `main` with the pull-request gate and the protocol rewrite it forces, the backup posture for state git cannot hold, and the rule that decides where any future enforcement belongs. `phase-conc-09` is owner-executed: it applies an approved `AGENTS.md` change no agent may make | [Concurrency, git safety and enforcement](../01-plans/PLAN-026-concurrency-git-safety.md) and [requirements](../06-requirements/REQ-013-concurrency-git-safety.md) |
| `phase-idg-*` | Idea graph and lifecycle (`P1`) — the `ARCH-005` schema bundle (three classification axes, `component_of`, the `lineage` annotation, document-code link targets), the pure classification agent and the corpus backfill, tagging, the decomposition procedure, subagent and session-less capture, the connection-builder agent, and the idea-to-plan path: a plan-quality standard, where a promoted draft lives, and the planner agent measured against both. Distinct from `phase-idea-*`, which built the append-only log this track adds structure to | [Idea graph and lifecycle](../01-plans/PLAN-029-idea-graph-lifecycle.md) and [requirements](../06-requirements/REQ-014-idea-graph-lifecycle.md) |
| `phase-prog-*` | One phase per programme from the accepted idea partition. Each finalizes that programme's placeholder plan into a real requirement, design and implementation phases — it builds nothing itself. Twelve phases for `PLAN-026`–`PLAN-037`, queued in the owner's delivery order: `P3`, `P10`, `P11`, then `P1`, `P2`, `P4`–`P9`, `P12`. Read `next_up` for the order rather than the code sequence — `P10` and `P11` were swapped on 2026-09-14, after their codes were permanent | [The partition](../00-working/idea-batching-partition.md); each placeholder plan links its own source material |
| `phase-lrr-*` | Literature-review campaign report page — deterministic extraction of the fourteen campaign files, the template family and faithful Markdown rendering, the three browsable tables (ledger 1,200 rows, inventory 1,154, matrix 67 × 43), and cross-linking plus the epistemic framing the campaign's own rulings require. Renders `research/literature-review/`; never writes there | [Report page](../01-plans/PLAN-043-literature-review-report-page.md) and [requirements](../06-requirements/REQ-027-literature-review-report-page.md) |

```bash
uv run python -m src.governance --ready
uv run python -m src.governance --backlog
```

Read the [accepted decisions](../08-governance/GOV-003-backlog-decisions.md), [session workflow](../08-governance/GOV-002-backlog-protocol.md), and [initial coverage review](../08-governance/GOV-004-backlog-capture.md). The full report includes phase details and a source-plan coverage table. The YAML catalog holds state; this index holds navigation only.

Before starting a phase, validate the entire backlog. Capturing or approving a phase does not mean it is implemented. Deferred work remains visible with a resume condition.

`max_active` bounds how many phases may be claimed at once, and concurrent claims must target disjoint systems, disjoint deliverable paths and unrelated dependency chains. Each active phase names the `agent` holding it, which is also its branch name `agent/<phase-id>`. Both reports open with the active-claims table and mark, per phase, which active claims it would collide with. See [ADR-003](../04-decisions/ADR-003-multi-agent-concurrency.md) and the agent steps in [AGENTS.md](../../AGENTS.md).
