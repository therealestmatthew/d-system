schema_version: 1
updated: '2026-09-22'
# Ideas that jump the queue, in order. Everything else falls back to status then ID
# (open before triaged; lower ID first within a status). Hand-edited, like backlog.yaml's
# next_up — reordering is an operational decision, not a fact logged in _data/ideas.jsonl.
#
# Re-ranked 2026-09-22 by owner ruling, after backlog.yaml broke three times in one session
# and once reached a commit. 000240 takes position 1 and displaces the front held since
# 2026-09-12: three breakages in a day is a stronger claim on attention than work that has
# waited ten days without incident.
next_up:
# Front of the queue, 2026-09-22 by owner ruling.
#
# NOTHING CAN BE CLAIMED FOR THIS IDEA YET. It has no requirement, no plan and no backlog
# phase — only the idea. The next step is a planning pass that turns it into governed
# documents, not a session that starts writing tools/append_backlog.py. Ranking it first
# is what puts it in front of that planning pass.
#
# Why it is first: docs/09-backlog/backlog.yaml is the repository's lock table, 13,278
# lines, with no sanctioned writer — every edit is hand-rolled text surgery. It broke three
# times on 2026-09-22 (indentation inferred from yaml.safe_dump output; a plain multi-line
# scalar quoted at its first line only; a colon-space inside a plain scalar), and the third
# reached a commit because the verification ran in a script whose failure did not stop the
# git commit that followed. brain/procedures/edit-backlog-yaml-by-anchored-block.md records
# all three and is the interim guard; a guard that failed once already is not the fix.
#
# Related and deliberately NOT ranked here: 000224 (a whole-file write silently reverted
# four completed phases) is the damage this prevents, and its detection half already has a
# phase — phase-gov-05, ranked first in backlog.yaml's next_up the same day. Detection and
# prevention are different jobs; this is the prevention half.
- '000240'
# Held from 2026-09-12, each slid down one.
- '000157'
- '000158'
- '000195'
# Added 2026-09-21. Gates 000302 and 000304, which are annotated as held behind it.
- '000303'
# Repo-hygiene defects that bite agents rather than users, added 2026-09-21.
- '000241'
- '000284'
- '000283'
# Literature-campaign findings that outlive the campaign, added 2026-09-21.
- '000214'
- '000219'
# Held from 2026-09-12, unchanged in relative order.
- '000066'
- '000041'
- '000038'
- '000037'
- '000040'
- '000091'
