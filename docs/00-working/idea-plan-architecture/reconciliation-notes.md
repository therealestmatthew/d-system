# Pre-synthesis reconciliation

Exploratory category reasoning belongs here without metadata, per the current task and ADR-010.
The final durable contract will be one parent plan with six children and one requirement document.
These notes and the transcript analysis establish provenance; they are not implementation plans.
No draft category files will be governed merely to satisfy coverage.

The four requested folders are retained. Fourteen original categories shrink to six: idea lifecycle
and promotion (G/I); plan lifecycle (H); event/schema/identity/fold (A/B/C); annotations and links
(D/E); safe writing, read models, rendering and metrics (J/K/L/M); governance and delivery (N).
Tags/classification (F) become a deferral row with evidence gates, not a schema or child plan.
Historical preservation is shared through requirements, rather than a speculative repair project.

Run the complexity review alongside architecture, and before schema expansion. Its actual
prerequisites remain in force; it is queued but not ready today. Its no-new-structure restriction
applies to that review session. This explicitly authorized architecture adds documentation folders
using existing kinds/systems, and proposes extensions to the existing idea schema, not a new system.
Do not reorder next_up or execute the review as an undeclared extra task.

Carry all contradictions into the parent synthesis: clean folded display versus visible history;
required-field clearing versus retraction; nested amendment precedence; seq pointers versus sibling
worktree concurrency; phases-derived consistency versus human approval/closure; cancelled-phase
closure; immutable historical target validity versus present document state; metrics versus absent
transitions; plan history versus planned Git squash; L7 versus PLAN-016; no invented identity merge;
and incomplete transcript provenance. Recommendations stay labeled until resolved.
