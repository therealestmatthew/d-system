"""Graph definitions, one module per run kind. Only `intake.py` exists in this phase.

`batch.py`, `unit.py` and `realization.py` are `phase-irs-14`, `phase-irs-15` and the
backlog-phase unit graph's own deliverables (PLAN-039.01 section 12) — not stubbed here,
since an empty stub would assert a shape this phase has not designed.
"""
